## Fixed

- **Packaging:** added the missing `python-cairo` dependency to the PKGBUILD and
  the AUR package. Without it the GUI could fail to start on systems where
  `python-cairo` was not already pulled in by another package.
